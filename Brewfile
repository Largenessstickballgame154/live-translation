# Non-pip (system) dependencies. Install with:  brew bundle
brew "ffmpeg"            # audio decoding for whisper (required)
brew "ollama"            # local LLM server (optional translategemma backend)
cask "blackhole-2ch"     # virtual audio device to capture system audio
